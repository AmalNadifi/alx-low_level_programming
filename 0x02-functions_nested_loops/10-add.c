#include "main.h"
/**
 * add - The program adds two integers and returns the result
 * @x: first integer
 * @y: second integer
 * Return: the sum of the two integers
 */

int add(int x, int y)
{
	int z;

	z = x + y;

	return (z);
}
