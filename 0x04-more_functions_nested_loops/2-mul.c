#include "main.h"
/**
 * mul - The program computes the absolute value of an integer
 * @a: the first integer
 * @b: the second integer
 * Return: the multiplication result
 */

int mul(int a, int b)
{
	return (a * b);
}
